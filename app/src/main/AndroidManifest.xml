<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dahai.contentproviderdemo">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".util.ImagePickActivity"
            android:theme="@style/NoTitleThem"/>
        <activity android:name=".util.ImageShowActivity"
            android:theme="@style/NoTitleThem"/>
        <activity android:name=".util.PreviewImageActivity"
            android:theme="@style/NoTitleThem"/>
        <activity android:name=".util.PreviewImageActivity2"
            android:theme="@style/NoTitleThem"/>
    </application>

</manifest>